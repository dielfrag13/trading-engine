#include "strategies/MovingAverage.hpp"

// Implementation is header-only for now (logic lives in the header). This file
// is kept to satisfy the build system if we want to add non-header code later.
